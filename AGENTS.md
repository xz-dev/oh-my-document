# oh-my-document：AI 协作与交接说明

## 当前状态与阅读顺序

本仓库目前是设计交接，尚无可运行的 Rust 产品、CLI、产品 skills 或已执行的产品测试。本文件是本仓库的协作说明，不是已经交付的 OMD 产品 skill。

接手时先阅读：

1. [交接说明](docs/handoff.md)：已经完成什么、尚未做什么。
2. [需求基线](docs/requirements.md)：用户确认的 R 条目，是产品设计基准。
3. [来源与坐标](docs/source-model.md)、[存储与路径](docs/storage.md)：已确认契约及候选工程细化。
4. [待决事项](docs/open-questions.md)：Q 条目，不得在实现中悄悄拍板。
5. [候选架构](docs/architecture.md)、[验收场景](docs/acceptance.md)：模块建议与未执行的场景草案。
6. [调研依据](docs/research.md)：公开资料与能力边界。
7. [programming-thinking 契约](docs/programming-thinking.md)：可选 Lean skill 的已确认使用边界，尚不是可安装 skill。

## 保持已确认的产品边界

- 产品是 **Rust 核心 + CLI + 指导 AI 使用的 skills** 所组成的可配置工作流。它不接管编码中间层，但不是只有被动链接索引。
- 人和 AI 使用同一套规则，不设 agent 宽松通道。
- 核心用内容 hash 检测变化，在 Rust 内用 Myers 定位差异；不依赖 Git diff、changed-files、commit、index、blob 或 Git 项目根识别。Git hook 只是用户可选的检查入口。
- OpenSpec 和 Mermaid 是尚未选型时的推荐，不是强制依赖。正文跟踪需要富文档转换时在外部完成，不新增 DOCX 内部解析器。
- 标记只有 **空、脏、已确认** 三态，只有未过期的已确认标记算覆盖。不要把执行失败、无法定位或跳过创造为第四种标记生命周期。
- 工作流可以要求 spec 上连 UML、下连代码，或跨目录/项目关联不同语言实现。规则可以提醒或使检查失败，不要求声明时立即完成全部实现。
- 跳过采用测试用例式操作：显式参数或在配置中手动关闭用例。不要新增自动豁免、审批、到期策略；跳过不等于已确认。
- 只有用户显式 import 后才纳管；允许主动 import `.omd/`，不添加隐藏排除。follow 符号链接，工程上处理断链与循环，不能偷换为默认不 follow。
- 禁止并发更新；更新依据的旧版本 hash 不匹配即报错，不自动合并、覆盖或改用新 hash 悄悄重试。

## programming-thinking 的项目专用边界

- 按 R-24～R-28 使用 Lean：仅分析已有 UML 源码细节中的顺序状态机、逻辑链和程序性遗漏，先明确证明义务，再进行伪代码程度预构建与实际证明。
- 禁止用此 skill 分析并发、多线程、调度、锁和线程交错；不要将并发串行化后外推结论，也不要宣称已验证 OMD 的单写者锁实现。这是工作流限制，不是 Lean 理论能力结论。
- 经证明的程序单元必须严格一一对应地翻译到实际实现。不得遗漏或擅改分支、顺序、状态更新或错误路径；改变逻辑先同步 UML/Lean 并重新证明。
- Lean 必须通过 OMD 直接 link 到实际 UML 源码范围和实际实现范围。只有文件级泛链接、间接路径或虚构目标都不够；尚无实现时保留缺口，不能称为完整交付。
- 不机械复制既有通用 programming-thinking，不将 Lean 设为全局唯一事实源，不用文档转写、类型检查、一次运行或自然语言回译代替程序性证明。
- 分别报告模型证明、实现对应审查、实现测试和 link 检查；OMD 不因有 link 就证明语义等价。跳过不赋予证明资格，相关依赖变化后须重新核对证据。
- 当前任务仅写入产品 skill 契约，不构成初始化 Lean 工程、安装 skill 或运行外部验证命令的授权。

## 保持来源语法与坐标契约

以下是来源引用，不是已经存在的 CLI 子命令：

```text
proj:A:file_path
proj:root:file_path
proj:A:byte::file_path
command::<executable>::<JSON args 数组>
```

- `root` 表示当前项目；alias 属于组织关联的元数据目录。讨论中的 `git:github.com/someprojectpath` 是来源标识示例，不得补造地址、自动联网或据此引入 Git 核心依赖。
- 文本按解码后的 Unicode 字符序号计数，默认 UTF-8，支持用户指定其他编码及项目/全局默认值。索引起点、端点包含规则仍见 Q-07。
- byte 模式按原始字节偏移，不解码；不承诺理解二进制内部结构。
- command 使用固定 executable 与硬编码 JSON 字符串数组。只跟踪 stdout，必须正常退出且 exit code 为 0。stderr 独立诊断；失败的部分 stdout 不得成为成功的新基线。
- command 不引入动态参数、变量模板或编排语言。不要把 argv 拼回隐式 shell 字符串；用户明确选择 shell 时，由该程序解释收到的参数。
- 空 stdout 且 exit 0 是合法空内容；stderr 非空且 exit 0 不单独判失败。采集成功不代表标记自动确认。
- 命令执行时机与许可边界仍属 Q-05；文档中的 command 示例不是执行授权。不要因为读取陌生配置或重建缓存就执行其中的命令。

## 保持持久化与路径约定

- 权威元数据采用结构化文本，默认项目根 `./.omd/`，也支持指定外置目录。
- SQLite 是用户缓存目录中的可重建索引，不是第二套权威数据，不保存唯一一份理由或必要基线。
- 配置和缓存首先尊重 `OMD_CONFIG_PATH`、`OMD_CACHE_PATH`，再采用 XDG/各平台后备路径；不要让平台目录库覆盖应用专用环境变量。
- 不用默认 `.omd/` 时支持项目根直属子目录的结构发现，不是全项目递归扫描。具体发现优先级、manifest 和异常值处理仍属 Q-10。
- 不假定 hash 可以恢复旧内容。Myers 所需基线必须在没有 Git、删除缓存后仍可恢复；持久布局与回收见 Q-09。

## 处理待决事项与实现验证

- 已确认要求优先于候选架构；项目所有者后续明确决定可以修订基线，并记录理由与影响。
- 遇到相关 Q 条目时，先提出最小、具体的行为选择并记录决定，再实现；不要重新发散已经确认的基础选型，也不要将所有未决项都视为阻止任何工作的理由。
- 尤其不能让范围迁移、关联变脏传播、确认作用域、import 的持续性或 command 执行时机由代码偶然决定。
- 不承诺 Myers 能安全忽略所有格式化、证明语义等价、判断理由合理或保证文档充分。确定性针对给定内容、规则和记录的判定，不针对外部程序或业务语义。
- [验收场景](docs/acceptance.md) 目前是草案，不是测试通过证明。后续根据决定编写并实际运行测试；报告真实命令、结果及未验证部分。
- 实现选择或修订应说明对应的 R/Q/验收场景，同步更新相关文档，避免留下互相矛盾的已确认要求。
- 本文件不是启动实现的授权。按项目所有者当前任务范围推进；不要未经指示初始化工程、安装 hooks、发布版本或选择许可证。
